<h1>Streams  Users</h1>
<p>Find me in app/views/streams/index.html.erb</p>

<% @users.each do |user| %>
    <div>
        <h2>[<%= user.id %>] <%= user.username %></h2>
        <p><%= link_to user.username, channel_path(user) %></p>
    </div>
<% end %>


